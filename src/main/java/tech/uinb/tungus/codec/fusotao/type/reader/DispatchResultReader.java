package tech.uinb.tungus.codec.fusotao.type.reader;

import tech.uinb.tungus.codec.fusotao.type.DispatchResult;
import io.emeraldpay.polkaj.scale.ScaleCodecReader;
import io.emeraldpay.polkaj.scale.ScaleReader;

public class DispatchResultReader implements ScaleReader<DispatchResult> {
    @Override
    public DispatchResult read(ScaleCodecReader rdr) {
        return null;
    }
}
